# March 19, 2026

## StrategyPreferreds Website Fixes

Fixed multiple critical issues with strategy-preferreds-deploy/index.html:

1. **JavaScript syntax errors** - Fixed numerous issues:
   - parseFloat(price.toFixed(2))} → parseFloat(price.toFixed(2))}
   - Duplicate zoom config in Chart.js options
   - totalPeriods i++ → totalPeriods; i++
   
2. **Calculator math fixed** - Now uses quarterly compounding correctly for STRK/STRD/STRF (STRC is monthly). Formula:
   - periodsPerYear = 12 for STRC, 4 for others
   - divPerPeriod = divRate / periodsPerYear
   - Loop through totalPeriods, reinvesting dividends if DRIP is on

3. **Chart timeline buttons fixed** - setRange() function now works properly with 3M/6M/YTD/ALL buttons

4. **Added STRE back** - EU-denominated preferred stock, hidden by default

5. **Dividends tab updated**:
   - Shows Yield at Par (badge) and Effective Yield (orange)
   - Removed "rate per mo/qtr" column
   - Added "Get In By" column with record dates

6. **Added Beta Match tab** - New calculator to match portfolio to target volatility using stock + BTC allocation

File location: ~/.openclaw/workspace/strategy-preferreds-deploy/index.html
